<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fullcourse.wishlist.mapper.WishlistMapper">

	<select id="selectAll" resultType="com.fullcourse.wishlist.WishlistVO">
		select * from wishList where wishListId=1 order by wishListNum desc
	</select>

	<select id="selectAllTour"
		resultType="com.fullcourse.wishlist.WishlistViewVO">
		select wishlist.wishListNum, wishlist.wishListId, tour.tourNum,
		tour.tourAddress, tour.tourContent, tour.tourUsetime
		from tour, wishlist where tour.tourNum = wishlist.wishListTourId and
		wishListId=1
	</select>

	<select id="selectAllFestival"
		resultType="com.fullcourse.wishlist.WishlistViewVO">
		select wishlist.wishListNum, wishlist.wishListId,
		festival.festivalNum, festival.festivalName, festival.festivalAddress,
		festival.festivalContent,
		festival.festivalStart, festival.festivalFinal, festival.festivalUsetime
		from wishlist, festival where festival.festivalNum =
		wishlist.wishListFestivalId and wishListId=1
	</select>

	<select id="getTotalRows" resultType="int">
		select count(*) total_rows from wishList where wishListId=1;
	</select>


	<insert id="insertOK" parameterType="com.fullcourse.wishlist.WishlistVO">
		insert into
		member(id,pw,name,tel)
		values(#{id},#{pw},#{name},#{tel})
	</insert>

	<update id="updateOK" parameterType="com.fullcourse.wishlist.WishlistVO">
		update member set pw=#{pw},name=#{name},tel=#{tel}
		where num=#{num}
	</update>

	<delete id="deleteOK" parameterType="com.fullcourse.wishlist.WishlistVO">
		delete from member where num=#{num}
	</delete>


	<select id="selectOne" resultType="com.fullcourse.wishlist.WishlistVO">
		select * from member where
		num=#{num}
	</select>

	<select id="searchListID"
		resultType="com.fullcourse.wishlist.WishlistVO">
		select * from member where id like #{searchWord}
	</select>

	<select id="searchListNAME"
		resultType="com.fullcourse.wishlist.WishlistVO">
		select * from member where name like #{searchWord}
	</select>


	<select id="searchListID_PAGE"
		resultType="com.fullcourse.wishlist.WishlistVO">
		select * from member
		where id like #{searchWord}
		order by num desc limit #{startRow},#{pageBlock}
	</select>

	<select id="searchListNAME_PAGE"
		resultType="com.fullcourse.wishlist.WishlistVO">
		select * from member
		where name like #{searchWord}
		order by num desc limit #{startRow},#{pageBlock}
	</select>


	<select id="search_total_rows_id" resultType="int">
		select count(*) total_rows from member where id like #{searchWord}
	</select>

	<select id="search_total_rows_name" resultType="int">
		select count(*) total_rows from member where name like #{searchWord}
	</select>

</mapper>