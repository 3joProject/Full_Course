<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fullcourse.parking.mapper.ParkingMapper">

	<resultMap id="parkingResultMap" type="com.fullcourse.parking.ParkingVO">
		<result property="prkCenterId" column="prkCenterId" />
		<result property="prkPlceNm" column="prkPlceNm" />
		<result property="prkPlceAdres" column="prkPlceAdres" />
		<!-- format(prkPlceEntrcLa,6)으로 오는 값을 prkPlceEntrcLa 변수에 매핑 -->
		<result property="prkPlceEntrcLa" column="format(prkPlceEntrcLa,6)" />
		<!-- format(prkPlceEntrcLo,6)으로 오는 값을 prkPlceEntrcLo 변수에 매핑 -->
		<result property="prkPlceEntrcLo" column="format(prkPlceEntrcLo,6)" />
		<result property="prkCmprtCo" column="prkCmprtCo" />
	</resultMap>

	<select id="marker"
		resultType="com.fullcourse.parking.ParkingVO" resultMap="parkingResultMap">
		SELECT prkCenterId, prkPlceNm, prkPlceAdres,
		format(prkPlceEntrcLa,6), format(prkPlceEntrcLo,6), prkCmprtCo FROM
		prksttusinfo
		where prkPlceEntrcLa BETWEEN #{lat} - 0.008 AND #{lat} + 0.008
		AND prkPlceEntrcLo BETWEEN #{lng} - 0.008 AND #{lng} + 0.008
	</select>


	<insert id="insertOK" parameterType="com.fullcourse.cart.CartVO">
		insert into
		cart(cartMid, cartProduct, cartSellerId, cartTitle,
		cartPrice, cartImage, cartContent, cartInventory, cartCount)
		values('user1', #{cartProduct}, #{cartSellerId}, #{cartTitle},
		#{cartPrice}, #{cartImage}, #{cartContent},
		#{cartInventory}, #{cartCount})
	</insert>

	<update id="updateOK" parameterType="com.fullcourse.cart.CartVO">
		update cart set cartCount=#{cartCount} where cartNum=#{cartNum}
	</update>

	<delete id="deleteOK" parameterType="com.fullcourse.cart.CartVO">
		delete from cart where cartNum=#{cartNum}
	</delete>

	<select id="chkWDuplCart" resultType="int">
		select count(*) from cart where cartMid='user1' and
		cartProduct=#{cartProduct};
	</select>

</mapper>