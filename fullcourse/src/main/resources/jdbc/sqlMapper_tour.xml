<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fullcourse.tour.mapper.TourMapper">
	
	<insert id="TourInsertOK" parameterType="com.fullcourse.tour.TourVO">
    INSERT INTO tour (tourName, tourAddress, tourContent, tourUsetime, tourTel, tourViews, tourThumbUp, tourImg)
    VALUES (#{tourName}, #{tourAddress}, #{tourContent}, #{tourUsetime}, #{tourTel}, #{tourViews}, #{tourThumbUp}, #{tourImg})
</insert>

	
	<update id="updateOK" parameterType="com.fullcourse.tour.TourVO">
		update member set pw=#{pw},name=#{name},tel=#{tel}
		where num=#{num}
	</update>
	
	<delete id="deleteOK" parameterType="com.fullcourse.tour.TourVO">
		delete from member where num=#{num}
	</delete>
	
	<select id="TourSelectAll" resultType="com.fullcourse.tour.TourVO">
		select * from Tour order by
		tourNum desc
	</select>
	
	<select id="TourSelectOne" resultType="com.fullcourse.tour.TourVO">
		select * from Tour where
		tourNum=2
	</select>
	
	<select id="searchListID"
		resultType="com.fullcourse.tour.TourVO">
		select * from member where id like #{searchWord}
	</select>

	<select id="searchListNAME"
		resultType="com.fullcourse.tour.TourVO">
		select * from member where name like #{searchWord}
	</select>
	
	<select id="selectAllPageBlock"
		resultType="com.fullcourse.tour.TourVO">
		select * from member 
		order by num desc limit #{startRow},#{pageBlock}
	</select>
	
	<select id="searchListID_PAGE"
		resultType="com.fullcourse.tour.TourVO">
		select * from member
		where id like #{searchWord} 
		order by num desc limit #{startRow},#{pageBlock}
	</select>
	
	<select id="searchListNAME_PAGE"
		resultType="com.fullcourse.tour.TourVO">
		select * from member
		where name like #{searchWord} 
		order by num desc limit #{startRow},#{pageBlock}
	</select>
	
	<select id="getTotalRows"	resultType="int">
		select count(*) total_rows from member
	</select>
	
	<select id="search_total_rows_id"	resultType="int">
		select count(*) total_rows from member  where id like #{searchWord}
	</select>

	<select id="search_total_rows_name"	resultType="int">
		select count(*) total_rows from member  where name like #{searchWord}
	</select>

</mapper>